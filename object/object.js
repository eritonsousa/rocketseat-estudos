const person = {
    name: 'Eriton',
    age: 22,
    weight: 69.5,
    isAdmin: true
}

console.log(person.name)
console.log(`${person.name} tem ${person.age} anos`),

/* OBJECTS precisa de como se fosse uma variável e atribuir um valor (name: 'Eriton) */